{
  "name": "hearthside-rooms",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: the optional private-room server for Hearthside Poker.",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.131.2"
  }
}
